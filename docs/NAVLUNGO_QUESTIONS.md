# Navlungo Provider Questions

- Which API version should we use for production: v2 or v2.1?
- What is the production base URL?
- Is v2.1 Beta production-ready?
- Is IP allowlist required for Render/cloud backend requests?
- Are there any TLS/SNI/firewall restrictions for cloud backends?
- Do you support return/reverse shipments in production?
- Does Create Post return tracking_url and barcode_url immediately?
- Is Barcode also available through a separate endpoint?
- How do we fetch carrier_id values?
- Can carrier_id=1 be used for automatic/by coverage area selection?
- Are shipment status webhooks supported?
- Can one account manage multiple sender addresses/warehouses?
- Can one account support marketplace/multi-vendor flows?
- Is there a safe sandbox/test account or should we test on live?
- Does live testing charge prepaid balance when using Create Post?
